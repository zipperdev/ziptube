extends base 

block content 
    if (errorMessage) 
        span=errorMessage
    form(method="POST") 
        input(placeholder="User Email", name="email", type="email", required) 
        input(placeholder="Password", name="password", type="password", required) 
        input(type="submit", value="Login") 
        br
        a(href="/users/github/login") Continue with Github
    hr 
    div 
        span Don't have an account? 
        a(href="/signup") Create one now