extends base

block content 
    if (errorMessage) 
        span=errorMessage
    form(method="POST") 
        input(placeholder="Current Password", name="cPassword", type="password", required) 
        input(placeholder="New Password", name="nPassword", type="password", required) 
        input(placeholder="Confrim New Password", name="nConfrimPassword", type="password", required) 
        input(type="submit", value="Change Password")